!!!
html
  head
    title= title
    link(rel='stylesheet', href='/css/bootstrap.min.css')
    link(rel='stylesheet', href='/css/bootstrap-responsive.min.css')
    link(rel='stylesheet', href='/css/app.css')
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    
  body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          a.brand(href='/') 
            img(src=bnImageUrl, style="height:25px;width:25px;")
            | &nbsp;BeautifulNode
          .nav-collapse
            ul.nav
              each item in menu
                li(class=item.class): a(href=item.href)= item.name
          .pull-right
            if loggedIn
              a.brand(href='#', data-delete="#logout") Logout
              form(id="logout", method="post", action="/sessions")
                input(type="hidden", name="_method", value="delete")
            else
              a.brand(href='/signup') Sign Up
              a.brand(href="/login") Login
    .container
      .span8
        != body
      .span3
        .well
          center: img(src=bnImageUrl)
          h1 BeautifulNode
          p Short Developer Reviews of NodeJs Modules
    script(src='http://cdnjs.cloudflare.com/ajax/libs/jquery/1.7.2/jquery.min.js')
    script(src='/js/bootstrap.min.js')
    script(src="/js/app.js")